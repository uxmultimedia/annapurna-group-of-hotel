"use client";

import { CalendarCheck, CheckCircle2, ClipboardList, MessageSquareText, PartyPopper } from "lucide-react";
import { motion } from "framer-motion";

const steps=[[MessageSquareText,"Send Enquiry","Tell us about your occasion and preferred date."],[CalendarCheck,"Venue Visit","Walk through the spaces with our events team."],[ClipboardList,"Select Package","Shape the menu, décor and services around your plans."],[CheckCircle2,"Confirm Booking","Secure the venue with clear final details."],[PartyPopper,"Celebrate Your Event","Arrive with confidence and enjoy every moment."]] as const;
export function BookingProcess(){return <section aria-labelledby="process-title" className="bg-[#f8f3e9] px-6 py-24 sm:px-10 lg:py-32"><div className="mx-auto max-w-[1280px]"><div className="text-center"><p className="text-[10px] font-semibold uppercase tracking-[.4em] text-[var(--emerald)]">Simple & Personal</p><h2 id="process-title" className="mt-5 font-[family-name:var(--font-cormorant)] text-[clamp(2.7rem,5vw,4.2rem)] font-medium text-[#173c2b]">From enquiry to celebration</h2></div><div className="relative mt-14 grid gap-5 lg:grid-cols-5"><div className="absolute left-[10%] right-[10%] top-7 hidden h-px bg-[#b69b60]/35 lg:block"/>{steps.map(([Icon,title,text],i)=><motion.article key={title} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.08}} className="relative rounded-[20px] border border-[#173c2b]/9 bg-white p-7 lg:border-0 lg:bg-transparent lg:p-3 lg:text-center"><span className="relative z-10 inline-grid size-14 place-items-center rounded-full border border-[#b69b60]/35 bg-white text-[var(--emerald)] shadow-md"><Icon size={20}/></span><p className="mt-5 text-[9px] font-bold uppercase tracking-[.16em] text-[#aa8d51]">Step {i+1}</p><h3 className="mt-3 font-[family-name:var(--font-cormorant)] text-[24px] font-semibold text-[#173c2b]">{title}</h3><p className="mt-2 text-[11px] leading-5 text-[#68736c]">{text}</p></motion.article>)}</div></div></section>}
